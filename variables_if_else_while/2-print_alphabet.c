#include <stdio.h>

/**
 * main - entry point
 *
 * description:
 *
 * Return: Always 0 (Success)
 */

int main(void) {
	char alphabet = 'abcdefghijklmnopqrstuvwxyz';

	putchar(alphabet);

	return (0);
}
